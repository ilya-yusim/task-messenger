// Skill 1: String Reversal
// Location: skills/builtins/StringReversalSkill.fbs

namespace TaskMessenger.Skills;

// Uses [int8] vectors for uniform pointer-based access pattern
table StringReversalRequest {
  input: [int8];  // String bytes (not null-terminated)
}

table StringReversalResponse {
  output: [int8];  // String bytes (not null-terminated)
  original_length: [uint32];  // Scalar stored as single-element vector for uniform pointer access
}
